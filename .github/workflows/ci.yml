name: CI-TITAN

on:
  pull_request:
    branches:
      - titan-master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Install dependencies
        run: chmod +x ./gradlew dependencies

      - name: Build project
        run: chmod +x ./gradlew build

      - name: Run tests
        run: chmod +x ./gradlew test

      - name: Lint code
        run: chmod +x ./gradlew check